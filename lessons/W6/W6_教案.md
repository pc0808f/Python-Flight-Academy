# W6：變數與運算應用

**課程名稱**：飛行驅動 Python - Week 6
**授課時數**：3 小時（150 分鐘）
**適用對象**：高中學生
**教學日期**：____________
**授課教師**：____________

---

## 一、教學目標

### 知識目標
1. 理解變數更新運算（+=, -=, *=, /=）
2. 掌握變數在飛行控制中的應用
3. 學習動態參數調整技巧
4. 認識全域變數與區域變數

### 技能目標
1. 能使用變數儲存飛行狀態
2. 能動態調整飛行參數
3. 能設計參數累加/遞減程式
4. 能整合前面所學完成複雜任務

### 態度目標
1. 培養狀態管理思維
2. 建立動態調整概念
3. 養成變數命名好習慣

---

## 二、教學準備

### 教師準備
- [ ] 教學簡報（PPT）
- [ ] 變數更新運算對照表
- [ ] 範例程式碼
- [ ] 飛行參數記錄表
- [ ] 投影設備

### 學生準備
- [ ] 已完成 W5 作業
- [ ] Webots 環境正常
- [ ] 學習筆記本

---

## 三、教學流程（150 分鐘）

### 第一節：W5 回顧與變數更新運算（50 分鐘）

#### 1-1. W5 作業檢討（15 分鐘）

**優秀作業展示**：
- 十字飛行：檢查移動函數使用
- 正方形巡邏：檢查迴圈與參數
- 創意圖形：展示創意作品並投票

**常見問題**：

```python
# 問題：函數參數過多
def move(x, y, z, speed, time, mode):  # ❌ 太多參數
    pass

# 改進：合理分組
def move(x, y, speed=1.0):  # ✅ 精簡合理
    pass
```

---

#### 1-2. 變數更新運算（20 分鐘）

**基本運算複習**：

```python
# 傳統寫法
height = 10
height = height + 5  # height 變成 15
print(height)  # 15
```

**更新運算子**：

```python
# += 加法更新
height = 10
height += 5  # 等同於 height = height + 5
print(height)  # 15

# -= 減法更新
battery = 100
battery -= 20  # 等同於 battery = battery - 20
print(battery)  # 80

# *= 乘法更新
speed = 5
speed *= 2  # 等同於 speed = speed * 2
print(speed)  # 10

# /= 除法更新
distance = 100
distance /= 2  # 等同於 distance = distance / 2
print(distance)  # 50.0
```

**對照表**：

| 更新運算 | 等同於 | 範例 | 結果 |
|---------|--------|------|------|
| `a += 5` | `a = a + 5` | `10 += 5` | `15` |
| `a -= 3` | `a = a - 3` | `10 -= 3` | `7` |
| `a *= 2` | `a = a * 2` | `10 *= 2` | `20` |
| `a /= 2` | `a = a / 2` | `10 /= 2` | `5.0` |

**飛行應用範例**：

```python
# 電池電量遞減
battery = 100
print(f"初始電量：{battery}%")

battery -= 10  # 飛行消耗 10%
print(f"飛行後：{battery}%")

battery -= 10  # 再消耗 10%
print(f"再飛行後：{battery}%")

# 高度累加
current_height = 0
print(f"起始高度：{current_height}m")

current_height += 0.5  # 上升 0.5m
print(f"第一次上升後：{current_height}m")

current_height += 0.5  # 再上升 0.5m
print(f"第二次上升後：{current_height}m")
```

---

#### 1-3. 計數器模式（15 分鐘）

**什麼是計數器？**

```python
# 計數器：記錄某件事發生了幾次

count = 0  # 初始化為 0

# 每次發生時，加 1
count += 1
count += 1
count += 1

print(f"發生了 {count} 次")  # 3
```

**飛行次數計數器**：

```python
"""
記錄起降次數
"""
import time

takeoff_count = 0  # 起飛次數
landing_count = 0  # 降落次數

def takeoff():
    global takeoff_count
    takeoff_count += 1
    print(f"第 {takeoff_count} 次起飛")

def land():
    global landing_count
    landing_count += 1
    print(f"第 {landing_count} 次降落")

# 模擬飛行
for i in range(3):
    takeoff()
    time.sleep(2)
    land()
    time.sleep(1)

print(f"\n總計：起飛 {takeoff_count} 次，降落 {landing_count} 次")
```

**累加器模式**：

```python
# 累加器：累積數值

total_distance = 0  # 初始化為 0

# 每次移動，累加距離
total_distance += 1.0
total_distance += 1.5
total_distance += 2.0

print(f"總飛行距離：{total_distance}m")  # 4.5
```

---

### 第二節：動態參數控制（50 分鐘）

#### 2-1. 漸進式高度變化（20 分鐘）

**目標**：讓無人機逐步增加高度

```python
"""
W6 範例：漸進式高度上升
每次上升都比前一次更高
"""

import time
from controller import Robot

drone = Robot()
timestep = int(drone.getBasicTimeStep())

# 初始化馬達
motors = []
for i in range(1, 5):
    motor = drone.getDevice(f"m{i}_motor")
    motor.setPosition(float('inf'))
    motor.setVelocity(0)
    motors.append(motor)

# 初始高度參數
flight_time = 0.5  # 起始飛行時間
velocity = 55

print("=" * 50)
print("  漸進式高度上升")
print("=" * 50)

# 啟動馬達
for motor in motors:
    motor.setVelocity(velocity)

# 5 階漸進上升
for level in range(1, 6):
    print(f"\n第 {level} 階：飛行 {flight_time} 秒")

    time.sleep(flight_time)
    print(f"  📍 到達第 {level} 階")

    # 每次增加飛行時間
    flight_time += 0.3  # 增量更新

    time.sleep(0.5)  # 短暫停留

# 降落
print("\n🛬 開始降落...")
for motor in motors:
    motor.setVelocity(0)
time.sleep(3)

print("\n✅ 漸進飛行完成！")
print("=" * 50)

while drone.step(timestep) != -1:
    pass
```

**重點說明**：
```python
flight_time = 0.5

# 第一次：flight_time = 0.5
flight_time += 0.3  # 現在 = 0.8

# 第二次：flight_time = 0.8
flight_time += 0.3  # 現在 = 1.1

# 每次都增加 0.3，越飛越高！
```

---

#### 2-2. 電池管理系統（15 分鐘）

**模擬電池消耗**：

```python
"""
電池管理系統
模擬電池消耗與警告
"""

import time

# 初始電量
battery = 100
warning_level = 30
critical_level = 15

print("=" * 50)
print("  電池管理系統")
print("=" * 50)

def check_battery():
    """檢查電池狀態"""
    if battery <= critical_level:
        print(f"⚠️  電量危險！({battery}%) 必須立即降落！")
        return False
    elif battery <= warning_level:
        print(f"⚡ 電量偏低 ({battery}%) 建議返航")
        return True
    else:
        print(f"✅ 電量充足 ({battery}%)")
        return True

def fly_mission(duration, consumption):
    """執行飛行任務"""
    global battery

    print(f"\n執行任務（{duration} 秒）...")

    # 檢查電量
    if not check_battery():
        print("❌ 任務取消：電量不足")
        return False

    # 執行任務
    time.sleep(duration)

    # 消耗電量
    battery -= consumption
    print(f"任務完成，消耗 {consumption}% 電量")

    return True

# 模擬多次飛行
missions = [
    (2, 15),  # 2 秒，消耗 15%
    (3, 20),  # 3 秒，消耗 20%
    (2, 15),
    (2, 15),
    (2, 15),
]

for i, (duration, consumption) in enumerate(missions, 1):
    print(f"\n{'='*50}")
    print(f"任務 {i}：")

    if not fly_mission(duration, consumption):
        break

    if battery <= critical_level:
        print("\n⚠️  電量過低，終止所有任務！")
        break

print(f"\n最終電量：{battery}%")
print("=" * 50)
```

---

#### 2-3. 速度漸變控制（15 分鐘）

**加速與減速**：

```python
"""
速度漸變控制
模擬加速起飛與減速降落
"""

import time
from controller import Robot

drone = Robot()
timestep = int(drone.getBasicTimeStep())

# 初始化馬達
motors = []
for i in range(1, 5):
    motor = drone.getDevice(f"m{i}_motor")
    motor.setPosition(float('inf'))
    motor.setVelocity(0)
    motors.append(motor)

print("=" * 50)
print("  速度漸變控制")
print("=" * 50)

# 加速起飛
print("\n🚀 加速起飛...")
velocity = 30  # 起始轉速

while velocity <= 55:
    print(f"  轉速：{velocity}")

    for motor in motors:
        motor.setVelocity(velocity)

    time.sleep(0.5)
    velocity += 5  # 每 0.5 秒加速 5

print("  ✓ 到達巡航速度")

# 巡航
time.sleep(2)

# 減速降落
print("\n🛬 減速降落...")

while velocity >= 0:
    print(f"  轉速：{velocity}")

    for motor in motors:
        motor.setVelocity(velocity)

    time.sleep(0.5)
    velocity -= 10  # 每 0.5 秒減速 10

    if velocity < 0:
        velocity = 0

print("  ✓ 安全著陸")
print("\n✅ 平穩飛行完成！")
print("=" * 50)

while drone.step(timestep) != -1:
    pass
```

---

### 第三節：綜合應用與挑戰（50 分鐘）

#### 3-1. 學生實作練習（25 分鐘）

**練習 1：飛行記錄器（基礎）**

```python
"""
任務：記錄飛行數據
要求：
1. 記錄起降次數
2. 記錄總飛行時間
3. 計算平均每次飛行時間
4. 輸出統計報告
"""

takeoff_count = 0
total_flight_time = 0

def takeoff_and_fly(duration):
    global takeoff_count, total_flight_time

    # TODO：更新計數器和累加器
    # TODO：執行飛行

    pass

# 測試
takeoff_and_fly(2)
takeoff_and_fly(3)
takeoff_and_fly(2)

# TODO：計算並輸出統計
```

**練習 2：自適應飛行（進階）**

```python
"""
任務：根據電量自動調整飛行時間
規則：
- 電量 > 50%：飛行 3 秒
- 電量 30-50%：飛行 2 秒
- 電量 < 30%：飛行 1 秒
"""

battery = 100

def adaptive_flight():
    global battery

    # TODO：根據電量決定飛行時間
    # TODO：消耗相應電量
    # TODO：輸出飛行資訊

    pass

# 執行多次飛行
for i in range(10):
    adaptive_flight()
    if battery < 15:
        break
```

**練習 3：螺旋上升（挑戰）**

```python
"""
任務：模擬螺旋上升飛行
要求：
1. 高度逐漸增加
2. 每圈略小旋轉（用時間模擬）
3. 飛 5 圈
4. 輸出每圈的高度
"""

height = 0
lap_time = 2.0

for lap in range(1, 6):
    # TODO：上升並旋轉
    # 提示：height += increment
    # 提示：lap_time 可以變化

    pass
```

---

#### 3-2. 飛行挑戰賽（15 分鐘）

**挑戰 1：最省電飛行**

```
目標：完成任務且消耗最少電量
任務：
- 起飛 3 次
- 每次懸停 2 秒
- 記錄總電量消耗

評分：
- 任務完成度
- 電量消耗（越少越好）
- 程式設計
```

**挑戰 2：智慧能源管理**

```
目標：設計智慧電池管理系統
要求：
- 監控電量
- 自動警告
- 低電量自動返航
- 記錄所有飛行數據

評分：
- 功能完整性
- 警告機制
- 數據記錄
```

---

#### 3-3. 作業說明與課程總結（10 分鐘）

**本週作業**：

**作業 1：飛行統計系統（必做）**
```
撰寫一個記錄飛行數據的程式：
1. 記錄起飛次數
2. 記錄總飛行時間
3. 記錄電量消耗
4. 輸出完整統計報告

要求：
- 使用計數器和累加器
- 使用更新運算（+=, -=）
- 輸出格式清晰
```

**作業 2：電池管理器（必做）**
```
設計一個電池管理系統：
1. 初始電量 100%
2. 每次飛行消耗 15-20%
3. 電量 < 30% 時警告
4. 電量 < 15% 時強制停止
5. 記錄可飛行次數

評分標準：
- 電量計算正確（40%）
- 警告機制（30%）
- 程式邏輯（30%）
```

**作業 3：動態飛行系統（選做，加分）**
```
設計一個動態調整飛行參數的系統：
1. 高度逐漸增加（漸進式）
2. 或速度逐漸增加（加速式）
3. 或時間逐漸增加（耐力式）
4. 記錄並輸出所有數據

創意加分：
- 設計獨特的變化模式
- 視覺化數據輸出
- 整合多種動態變化
```

**繳交方式**：
- 程式檔案：`W6_學號_姓名.py`
- 數據報告：`W6_學號_姓名_報告.txt`
- 繳交期限：下週上課前

---

**課程總結**：

**今天學到的重點**：
1. ✅ 變數更新運算（+=, -=, *=, /=）
2. ✅ 計數器與累加器模式
3. ✅ 動態參數控制
4. ✅ 狀態管理應用

**重要概念複習**：

```python
# 更新運算
count += 1      # 計數
total += value  # 累加
battery -= 10   # 遞減

# 動態參數
height = 0.5
for i in range(5):
    fly(height)
    height += 0.2  # 逐次增加
```

**下週預告**：
- 🔄 旋轉控制（Yaw）
- 🎯 組合高度、移動、旋轉
- ✈️ 設計複雜的 3D 飛行路徑
- 🏆 第一階段總結挑戰

---

## 四、教學評量

### 作業評分標準

| 項目 | 優秀 (90-100) | 良好 (80-89) | 及格 (70-79) | 待加強 (<70) |
|-----|--------------|------------|------------|-------------|
| 變數使用 | 正確使用更新運算 | 有使用變數 | 變數使用不當 | 未使用變數 |
| 邏輯正確性 | 計算完全正確 | 小錯誤 | 多處錯誤 | 邏輯錯誤 |
| 功能完整性 | 所有功能正常 | 大部分功能 | 部分功能 | 功能不全 |
| 程式碼品質 | 清晰、註解完整 | 尚可、有註解 | 混亂、少註解 | 難以理解 |

---

**教案版本**：v1.0
**編撰日期**：2026-01-30
**編撰者**：Python Flight Academy 團隊
